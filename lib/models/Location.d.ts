export declare class LocationCircle {
    id: string;
    lat: number;
    lon: number;
    radius: number;
    constructor(id: string, lat: number, lon: number, radius: number);
}
export declare type Location = LocationCircle;
